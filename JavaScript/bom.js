// bom objects
// windowobject
window.open();
var newwindow = window.open("http://google.com", "newwindow");
window.close();
// Screenobject
 screen.width;
 screen.height;
screen.availweight;
// navigator Object
function navigatorProp(){
    document.write("<br> appcodeName:"+ navigator.appCodeName);
    document.write("<br> appname:"+ navigator.appName);
    document.write("<br> platform:"+ navigator.platform);
}
navigatorProp();
